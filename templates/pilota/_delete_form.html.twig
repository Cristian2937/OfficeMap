<form method="post" action="{{ path('app_pilota_delete', {'id': pilotum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pilotum.id) }}">
    <button class="btn">Delete</button>
</form>
