<form method="post" action="{{ path('app_persona_giuridica_delete', {'id': persona_giuridica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona_giuridica.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>
